import Vuex from 'vuex'
import Vue from 'vue'
import global from './global'

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    global
  }
});
